---
description: Deploy the Nova submission worker to Cloudflare
---

Deploy the Nova worker:

1. Run `cd tools/nova && npx wrangler deploy` to deploy the worker
2. Report the deployed URL and version ID
