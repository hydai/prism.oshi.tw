import { UpNextSection } from 'prism-oshi-tw';

// Context-only component: reads the seeded 5-track queue from PlayerContext and
// renders the "Up Next" list (numbered 2–6, with per-track durations).
export const Default = () => <UpNextSection />;
